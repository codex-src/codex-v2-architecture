{
  "name": "codex-editor-prototypes",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "emoji-trie": "https://github.com/codex-src/emoji-trie.js",
    "lodash": "^4.17.15",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.4.1",
    "uuidv4": "^6.0.6"
  },
  "scripts": {
    "start": "flow && react-scripts start",
    "build": "flow && react-scripts build",
    "test": "flow && react-scripts test",
    "eject": "flow && react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "debug.css": "https://github.com/codex-src/debug.css",
    "flow-bin": "^0.122.0",
    "raw.macro": "^0.3.0",
    "serve": "^11.3.0"
  }
}
